# 削減候補 — 「削れるか」reviewer の初回実行結果

*測定: 2026-08-20 / 対象: `src/rc_basics_lab/` 22,928 行 + `tests/` 24,841 行 (サイクル 01〜05 完了時点)*
*観点の定義: `.claude/agents/reviewer-deletion.md` / 合否判定: `tests/test_artifact_invariance.py` (D-74)*

---

## 0. この文書の位置づけ

`docs/agent-system-review-from-artifacts.md` 1 節が指摘した
「評価軸に読み手が一度も入っていない / 削除・統合を要求した findings が0件」に対する、
**初めての「削れるか」観点のレビュー結果**である。

合否判定は新設していない。既にある成果物のバイト不変検査 (D-74) を、
これまでの「守る」側ではなく**「攻める」側**の判定に使う。

**合計の削減見積り: 約 1,630 行** (振る舞い不変・成果物バイト不変の範囲で)。

---

## 1. オーケストレータによる裁定

reviewer の 12 件のうち **1 件の severity を下げた**。

| id | reviewer | 裁定 | 理由 |
|---|---|---|---|
| AST 判定器の重複 | HIGH | **MEDIUM へ降格** | 「他6版は module-level しか見ないので D-59 の要件が守られていない」は**誤り**。実物を読むと `ast.walk(tree)` を使っており、これは関数本体も再帰的に走査する。重複 (正規化が3通りに割れている) は本物なので提案自体は残すが、正しさの問題ではない |

他の11件は実測の裏付けを確認した。特に最大の2件は独立に再現済み:

- `experiment/__init__.py` は **466 行 / `__all__` 198 名**。
  `from rc_basics_lab.experiment import ...` の呼び出しを全部見たところ、
  引かれているのは **モジュール名だけ** (`runner` / `narma` / `freerun` / `attractor` /
  `stability` / `threshold`)。**198 シンボルの利用は0件**
- docstring 内の rationale 常駐は 97 段落 / 742 行 (strict 判定)

---

## 2. 削減候補 (削減量の大きい順)

| # | 対象 | 削減 | severity | 種別 |
|---|---|---|---|---|
| 1 | docstring に常駐した rationale (97段落) | **742** | HIGH | 常駐 rationale |
| 2 | `experiment/__init__.py` の手書き再エクスポート | **383** | HIGH | 公開面 |
| 3 | 他6パッケージの同型の空回り | **120** | HIGH | 公開面 |
| 4 | `write_*_csv` 9本の共通化 | **85** | HIGH | 重複 |
| 5 | `tests/wiring.py` の機構が3〜4ファイルに写経 | **80** | HIGH | 判定器の重複 |
| 6 | モジュール `__all__` の未使用 91 名 | **91** | MEDIUM | 公開面 |
| 7 | AST 判定器の重複 (7ファイル) | **45** | MEDIUM (降格) | 判定器の重複 |
| 8 | plotting の `_new_figure`/`_save`/空チェック/`type: ignore` | **45** | MEDIUM | 重複 |
| 9 | `to_summary` 6本の共通化 | **15** | MEDIUM | 重複 |
| 10 | `datasets` の Source クラス2本 | **40** | INFO | 重複 (05 追加分) |
| 11 | 区間切り出しヘルパ5本 | **12** | INFO | 重複 (05 が4・5本目を追加) |
| 12 | 参照0の定義2件 | **13** | INFO | 死にコード |

### 特に重要な発見 (文書に無かったもの)

**(a) 公開面がほぼ全パッケージで空回りしている**

`__init__.py` が再エクスポートするシンボル数 → root から実際に引かれている数:

| パッケージ | シンボル | 利用 |
|---|---|---|
| `experiment` | 198 | **0** |
| `diagnostics` | 23 | **0** |
| `plotting` | 21 | **0** |
| `readout` | 17 | **0** |
| `datasets` | 14 | **0** |
| `tasks` | 13 | **0** |
| `reservoir` | 3 | **0** |
| **`config`** | 54 | **50** ← 本物の facade。D-49 が固定。**対象外** |

`config` だけが境界として機能している。他は「次にモジュールを足す人が2箇所へ写経する義務」
だけを生んでいる (05 は anomaly 系5本ぶん40名超をここへ写経した)。

**(b) 05 が「もう1セット」を作った軸が1つある**

`write_*_csv` (11箇所) と `to_summary` (13箇所) は 05 では**増えなかった**。
一方で「range で表した区間を配列から切り出す」2行のヘルパは
01 に1本・04 に1本だったところに **05 が3本足して5本**になった
(`_rows` / `_slice` / `_mask_slice` と名前も3通りに割れている)。

**(c) `tests/wiring.py` は自分の docstring で写経を戒めているのに、その機構自体が写経されている**

> 実験ごとに写経すると 01 で効いている検出力が 02 で静かに落ちても誰も気づかない

と書いてあるファイル自身の `fingerprint` / `_changed_leaves` / `_round_trip` /
`_leaf_value` などが3〜4ファイルに複製されている (92行)。

---

## 3. 触ってはいけないものとして確定させたもの

reviewer には着手前に
`python3 ~/.claude/scripts/check_decisions.py --list-tests .claude/decisions.yaml`
を実行させ、**guard_test に載るテスト関数を対象外**とした。決定を守っている唯一の機械なので、
消すと決定が散文に戻る。

あわせて以下は提案から除外されている:
- `config` パッケージの `__all__` (D-49 が差分0で固定)
- テスト本体の重複 (テストは独立性のため重複してよい。対象は*判定器*の重複のみ)

---

## 3.5 合否判定の射程 (実測で判明した限界。**着手前に必ず読む**)

D-74 の指紋検査は **「コミット済みの成果物が編集されていないこと」** を測る。
**「再生成しても同じバイト列になること」は測っていない。**
成果物を生成する層 (plotting / CSV 書き出し) を触るときは、この差が効く。

実測 (実験01を一時ディレクトリへ再生成し、コミット済みと比較):

| 種別 | 再生成でバイト一致するか | 理由 |
|---|---|---|
| **PNG** | **する** (`fig_comparison.png` / `fig_state_space.png` とも指紋と完全一致) | matplotlib の出力はこの環境では再現的 |
| **CSV** | **しない** | 最終列が `wall_time_s` で実行ごとに変わる。**それ以外の列は完全一致** |

したがって:

- **plotting を触る変更は「再生成して指紋と一致するか」で検証できる。**
  実際、#8 (plotting の共通化) はこの方法で振る舞い不変を実証した
- **CSV 書き出しを触る変更 (#4) はそれができない。**
  `wall_time_s` 列を除いた比較が受け入れ条件になる。
  「再生成したら差分が出た」を理由に指紋を書き直してはいけない ——
  それは合否判定そのものを無効にする

再生成の例 (`results/` を汚さない):

```
uv run python experiments/01_what_is_rc/run.py \
  --config experiments/01_what_is_rc/config.yaml --out <一時ディレクトリ>
```

## 4. 着手順の方針

`docs/リファクタリング方針.md` §6 は step 0 (バイト不変ゴールデンテスト) → 1 (plotting) →
2 (docstring) の順を提案していたが、**step 0 は D-74 として実装済み**であり、
その後の順は上表の (a) を踏まえて見直す余地がある。

- **最初に効くのは #2 + #3 (公開面、計 503 行)**。0 利用を実測済みで機械的に安全、
  かつ「466 行の `__init__.py` に 198 名が並ぶ」状態は**読み手の負荷そのもの**である
- **#1 (742 行) は最大だが、97 段落の移送先を1つずつ判断する必要がある**。
  移送先が `docs/design.md` の場合は `tests/test_design_doc.py` の照合対象になる点に注意
- **#4 (CSV) は D-74 の効き目を確かめる最初の題材として良い**。
  生成される CSV がバイト単位で同一であることを、新しい指紋検査がそのまま判定する
