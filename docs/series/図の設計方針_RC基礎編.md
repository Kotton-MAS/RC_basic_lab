# 図の設計方針【RC基礎編・連載通し】

*作成: 2026-08-20 / 対象: `rc-basics-01` 〜 `05` のすべての図*
*関連: `docs/series/連載構成案_RC基礎編.md`（各記事の図の枚数目安）、`docs/series/rc-basics-survey.md`（引用すべき先行研究と定量参照点）*

---

## 目的

各記事の図が単体で流通しても、次の3つが**図の中だけで読める**ようにする。

1. 何を主張しているか
2. 先行研究のどこに位置するか
3. どの条件で測ったか

実装側（参照線の値・文献値との照合・原典未特定の明記）は既に整っている。足りているのに読者の目に届いていない、というのが現状の診断である。

### 現状で既にできていること（変えない）

- 参照線が4種類引かれている: NARMA10 の NMSE = 0.16 / 0.107、MC ≤ N、IPC_total ≤ N、Lorenz の λ_max = 0.9056
- `plotting/figures_capacity.py::_reference_lines` が、参照線のラベルが欠けたら**描画前に落とす**
- `diagnostics/lyapunov.py` が「文献値は照合専用、正本は数値推定」を設計で分離している（D-42）
- 原典未特定であることを図の注に明記している（`NARMA10_REFERENCE_NOTE`）

---

## FIG-1: 図タイトルは結論文で書く

タイトルは「何を描いたか」ではなく**「何が言えたか」**を書く。説明はサブタイトルか軸ラベルに逃がす。

- 良い例（現状の 2-C）: `実験 2-C: 入力を強くすると rho > 1 でも ESP は成立する`
- 直す例（現状の 3-B）: `実験 3-B: rho とリーク率で動く線形/非線形の配分`
  → `実験 3-B: rho を上げると非線形容量の割合は下がる`

先行研究の再実演・追試である図は、**サブタイトルに `著者 (年) の再実演` / `著者 (年) の対照を公平化した再実験` を必ず入れる**。2-C（`figures_esp.py` の副題）が既にこの形になっている。

---

## FIG-2: 各記事に「文献照合」を最低1枚置く

記事ごとに、自分の実測値と文献値を**同じ軸の上に置いた図**（または対照表）を1枚必ず用意する。列は固定する。

| 量 | 本実験値 | 文献値 | 出典 | 条件差（N・入力・指標・正則化） |
|---|---|---|---|---|

- 図にする場合は、文献値を破線・実測を点＋誤差棒で重ねる
- 「文献値が無い」も情報である。空欄ではなく**「文献値なし」と明示する**
- 引用元は `docs/series/rc-basics-survey.md` に既に整理されている。survey に書いてある定量参照点が図に落ちていない状態を無くす

---

## FIG-3: 参照線には出典を図の中に書く

凡例テキストに `著者 (年)` を含める。本文の注は切り取られると消えるので、注だけに置かない。

| 現状 | 変更後 |
|---|---|
| `上限 MC <= N = 200` | `上限 MC ≤ N (Jaeger 2002 / Dambre 2012)` |
| `上限 IPC_total = N (傾き1)` | `上限 IPC_total ≤ N (Dambre 2012)` |
| `参照 NMSE = 0.16 (非線形性なしの天井)` | 末尾に `[原典未特定]` を付す（図の注は残す） |

`plotting/labels.py::label` は ja / en の両方を必須にしているので、追加する凡例は英語版も同時に書く。

---

## FIG-4: 指標と正規化を文献の土俵に合わせる

**指標の混在を解消する。** 現状 NMSE（3-C）/ NRMSE（4-B）/ RNMSE（文献側）が混在している。文献と比較する図では軸ラベルに定義式を書くか、図の注に `NMSE = ...` を置く。

`figures_capacity.py` の「参照値が NMSE で流通しているから誤差指標を NMSE にする」という判断が**正解の型**である。同じ判断基準を他の図にも適用する。

**上限が遠いときは正規化軸にする。** 3-A 左パネルは対数軸で上限 200 と実測 10〜40 が離れすぎ、「上限からどれだけ遠いか」という肝心の情報が読めない。`MC / N` を第2軸に置くか、主軸を `MC / N` に変える。

**それでも空白が残るなら軸を破断する（2026-08-31 追記）。** 正規化しても空白は
縮まない —— 空白の高さは比（200 / 36 = 5.5 倍）で決まり、対数軸でも `MC / N` へ
の付け替えでも変わらないためである。実測すると左パネルの上 2/3 が空いていた。
上段に上限線だけ、下段に実測だけを置いて破断する（`plotting/broken_axis.py`）。
**第2軸は破断しても必ず残す** —— 破断した軸の「見た目の距離」は量ではないので、
隔たりを数値で読める軸が要る。破断は上限が実測の `BREAK_RATIO`（3.0）倍以上
離れたときだけで、近いときは分割しない（空白が無いのに軸を割ると、読者は無い
はずの不連続を探す）。

---

## FIG-5: 色の意味を連載通しで固定する

手法・条件と色の対応を `plotting/style.py` に定数で持ち、5記事すべてで同じ色を使う。読者は記事をまたいで同じ対照群を見る。

- **手法（カテゴリ）**: 線形 / 遅延線（リッジ） / 遅延線（OLS・先行再現） / ESN — 固定4色
- **連続量（rho・リーク率・N のスイープ）**: viridis 系で統一
- **参照線・理論上限**: 常に同一色＋破線。データ系列と絶対に同色にしない

現状は 3-A（viridis）/ 3-B（tab:blue, tab:orange）/ 3-B'（独自）/ 3-C・4-B（単色）でバラバラ。

---

## FIG-6: 図に再現条件を焼き込む

`meta.json` にある情報が、図単体になった時点で失われている。図の隅（footnote 位置）に小さく1行入れる。

```
N=50, sigma_u=0.2, seeds=0-9 (10 rep), commit=abc1234
```

タイトルに `(N = 200, sigma_u = 0.1)` を入れている図が既にあるので、**それを全図共通の footnote に移して機械生成する**。手書きにすると図ごとに揃わない。

---

## FIG-7: 打ち切り・欠測を「データ」に見せない

`fig_ipc_profile.png`（3-B）は面積の約7割が 0 で、見えている矩形ブロックは打ち切り設定 `{(1,60),(3,10),...}` の形そのものである。読者はこれを系のデータ構造と誤読する。

- 打ち切り境界を白線か枠で明示し、境界外は「未計算」として 0 とは別の色（グレー）にする
- 遅延軸を対数にして、低遅延側の情報密度を上げる
- Dambre 2012 の定番は次数別の積み上げなので、3-B のスタックバーを主図、heatmap を補図に格下げすることも検討する

---

## 既存図の対応表

| 図 | 現状 | 対応 |
|---|---|---|
| `fig_esp_map` (2-C) | 結論文タイトル＋先行明記。**型のお手本** | 他図の基準にする。変更不要 |
| `fig_narma10_control` (3-C) | 参照線は有るが、**先行 (Goudarzi 2014) の条件が図に無い** | **最優先。** 「遅延線（正則化なし OLS）」を第4水準として追加し、先行の対照設計の穴を図で成立させる。Goudarzi の定量値（DL 1,810 タップ / train RNMSE 0.14 だが test は高いまま）も注か参照点に |
| `fig_valid_time` (4-B) | 文献の有効予測時間の参照線が無く、4.83 の良否が判断できない | Lorenz の valid time の文献値を参照線として追加（FIG-2 の照合図も兼ねられる） |
| `fig_mc_sweep` (3-A) | 対数軸で上限が遠すぎる | `MC/N` 第2軸 + **縦軸の破断**（FIG-4）、参照線に出典（FIG-3） |
| `fig_ipc_profile` (3-B) | 打ち切り形状の誤読リスク | FIG-7 に従い再設計 |
| `fig_memory_nonlinearity` (3-B) | 説明的タイトル、色が独自 | FIG-1、FIG-5 |
| `fig_ipc_conservation` (3-B') | 参照線の出典なし | FIG-3 |

### 3-C を最優先にする理由

`docs/series/rc-basics-survey.md` は実験 3-C の位置づけを、

- 旧: 「NARMA10 は線形で解けるのでは?」
- 新: 「**先行 (Goudarzi et al. 2014) の対照は正則化なし OLS だった。公平な対照で再実験する**」

に方向転換している。ところが現在の `fig_narma10_control.png` には Goudarzi の数字も、正則化なしの条件も出ていない。図が伝えているのは「遅延線 0.1538 < ESN 0.2673」だけで、**記事の主張が視覚的に成立していない**。同一図に OLS 版を並べて初めて「正則化を入れると結論が変わる」が図で言える。

---

## 実装上の置き場所

- 色・footnote 生成・参照線の描画は `plotting/style.py` と共通ヘルパに集約する。各 `figures_*.py` に散らすと FIG-5 が静かに崩れる
- `figures_capacity.py::_reference_lines` の「ラベルが欠けたら描画前に落とす」仕組みは正しいので、**出典文字列も同じ検証対象に含める**。値だけ足して出典を書き忘れる事故を落とす
- FIG-4 の指標選択（なぜ NMSE を採るか）は「普通はこうするが今回は理由があって別の選択をする」判断にあたるため、`.claude/decisions.yaml` に guard_test 付きで記録する

---

## 未解決

- Lorenz の有効予測時間について、参照線として引くべき文献値と条件（N・入力次元・観測ノイズ）の特定。`docs/series/rc-basics-survey.md` の「未解決の問い」に追加すること
- NARMA10 の参照値 0.16 / 0.107 の原典特定（survey 未解決1）。特定できるまでは `[原典未特定]` の表記を外さない

---

## 実装記録（2026-08-21・「実験を変えないもの」だけを実装した回）

この節は**実装者が仕様の外で決めたこと**の記録である。次に図を触る人はここを読む。
機械で守られる約束は `.claude/decisions.yaml` の **D-84〜D-88** にある（散文だけに残さない）。

### 実装した範囲

| 項目 | 適用先 | 実装 |
|---|---|---|
| FIG-1 | 全 21 図のうち 19 枚（2-C と 2-D は既に結論文/設計文なので据え置き） | 結論文へ書き換え。条件は footnote へ移した |
| FIG-3 | 3-A / 3-B' / 3-C の参照線 4 本 | 凡例に `[著者 年]` / `[原典未特定]`（D-84） |
| FIG-4 | 3-A 左パネル | `MC / N` を**第2軸**として追加 |
| FIG-5 | 作図層すべて | `style.METHOD_COLORS` / `SEQUENTIAL_CMAP` / `REFERENCE_COLOR`（D-85 / D-86） |
| FIG-6 | 全 21 図 | `style.add_provenance` による footnote（D-87） |
| FIG-7 | 3-B（`fig_ipc_profile`） | 打ち切り外をグレー + 境界線 + 遅延軸を対数（D-88） |

**やらなかったもの（実験が変わるため）**: 4-B への文献値の参照線（引くべき値と条件が未特定のため。下記「未解決」）。

**その後に実施したもの**:

- 3-C への「遅延線（正則化なし OLS）」の追加（D-90）。実測は OLS 0.1534 / リッジ 0.1538 で、
  **正則化は遅延線の優位の理由ではなかった**
- 3-C'（`fig_narma10_taps.png`）の新設（D-95）。上の結果が「動作点が先行と違うから」で
  あることを実測にした。`k / n_train` を 0.003 → 0.943 まで振ると、OLS / リッジ の NMSE 比が
  1.00 → **3.33** になる。先行の動作点（`k/n = 0.91`）を参照線で引いてある。
  **これが FIG-2（文献照合図）の第1号にあたる**
- FIG-2 の機械化（D-96）。`tests/test_figure_policy.py` の `KNOWN_WITHOUT_CITATION` で
  「文献照合図を持たない実験の集合」を凍結した。**増やせない・減らしたら外す**。
  実測: 現在持っているのは 03 だけで、01 / 02 / 04 / 05 は穴として記録してある
- 4-B のタイトルを実測に合わせた（D-96）。参照線が無いまま「数 Lyapunov 時間」と
  水準の良否を主張していたので、行から導いた手法間の差（ESN 4.83 = 線形の 70 倍）に
  変え、注に「文献照合は未了」と明記した
`fig_valid_time` の文献値は、この文書自身が「未解決」に置いている（条件の特定が先）。

### 仕様の外で決めたこと

1. **出典の書式は `本文 [著者 年]`**（角括弧）。丸括弧は既存のラベルが単位や条件に使っており（`(傾き1)` / `(a = 1.0)`）、混ざると出典が読み取れないため。
2. **出典を要求するのは文献由来の参照線だけ**とした。定義から出る線（2-A の丸めの床、2-B の `1/e` と `-1/log(1-a)`、01 の `NRMSE = 1`）は体裁（黒・破線）だけ揃え、出典は付けない。「定義」という出典を書くと `[原典未特定]` の重みが下がる。
3. **footnote に載せるのはレプリケート番号であってシード値ではない。** 行の `seed_*` 列は基底シードで、全レプリケートで同じ値である（実測: `capacity.csv` 117 行すべて `seed_reservoir=0`）。仕様の例 `seeds=0-9 (10 rep)` は `3 rep (replicate 0-2)` の形に読み替えた。
4. **footnote に commit を焼き込むと、PNG は生成時の HEAD に依存する。** D-74 の「再生成してバイト一致するか」で図の退行を判定する運用は、これで PNG にも効かなくなる（CSV の `wall_time_s`・`meta.json` の `timestamp_utc` は元から同じ性質で、PNG が最後のバイト安定な成果物だった）。**それでも焼き込む**という判断の理由は D-87 の rationale にある。
5. **参照線の色は黒に固定した。** 手法4色・05 の系統6色・04 の3態色・`SOURCE_STYLE` のいずれとも重ならない唯一の無彩色だったため。2本以上並ぶときは色ではなく破線の刻みで区別する。
6. **`delay_line_ols` は色だけ先に確保した。** 実験として足すのは今回の範囲外だが、後から色を挿すと既存4記事の配色がずれる。
7. **01 の PCA 図の空間色を、対応する手法の色に合わせた**（`入力の遅延埋め込み` = 遅延線の青、`リザバー状態` = ESN の緑）。記事をまたいで「どの特徴量の話か」が色で読める。
8. **FIG-4 は主軸ではなく第2軸にした。** 主軸を `MC / N` にすると上限線 `y = N` が `y = 1` になって「上限」という語と数字が結びつかない。第2軸なら実測値と割合の両方が読める。**N が1つに定まるときだけ引く**（格子に複数の N が混ざると `MC / N` が一意でない）。空白の解消は正規化ではなく**破断**で行う（2026-08-31）。
9. **3-B' (`fig_ipc_conservation`) だけは主軸を比にした。** こちらは上限が `IPC_total <= N` で N が動く掃引なので、絶対値のままだと上限線が**斜め**になり「上限からどれだけ離れたか」を斜めの隙間の目分量で測ることになる。比にすると上限が水平線 1 になり、距離を縦に読める。3-A は N が固定なので上限が水平線のままで、この問題が起きない —— **同じ FIG-4 でも掃引する軸が違えば結論が違う**。
9. **打ち切り（FIG-7）は設定から作図関数の引数として渡す。** 長形式（D-38）には正値セルしか無く、「未計算」と「しきい値を超えなかった 0」は行から区別できない。CSV に列を足すと成果物が変わるので採らなかった。引数を省略したときはマスクしない（打ち切りを知らない呼び出し側が「測っていない領域」を捏造しないため）。
10. **3-B のスタックバーを主図に格下げ・格上げする案は採らなかった**（FIG-7 の「検討する」）。図の入れ替えは記事構成の判断であって、実装者が決めることではない。

### 置き場所（FIG-5 の「共通ヘルパに集約する」の実体）

- `plotting/style.py`: 色（`METHOD_COLORS` / `SEQUENTIAL_CMAP` / `REFERENCE_COLOR` / `REFERENCE_DASHES`）と footnote（`add_provenance` / `footnote_text`）
- `plotting/labels.py`: 出典文字列（`JAEGER_2002` ほか）と `cited()`、手法名の `METHOD_LABELS`
- `plotting/heatmap.py`（新規）: 打ち切りのマスクと境界線、格子の復元（FIG-7）
- `plotting/capacity_grids.py`（新規）: 03 の「行 → 格子」の復元（描画を含まない）

新規モジュール2本は、`figures_capacity.py`（凍結 861 行）と `figures_esp.py`（凍結 770 行）が
D-77 のラチェットで1行も増やせないため、**描画でない部分を外へ出して場所を作った**もの。
結果として両者は 810 行 / 744 行に減り、ラチェットが1段締まっている。

---
---

# 第2版 — 実装後の再レビュー（2026-08-21）

*測定: ブランチ `feat/rc-basics-05`、HEAD `38412c0`*
*上の「既存図の対応表」は**初版の調査結果**である。現状は本節末の対応表が正本。*
*関連: `docs/process/agent-system-review-from-artifacts.md` 第3版 N〜Q 節（本節 FIG-8〜FIG-10 の元になった指摘）*

FIG-1 / 3 / 4 / 5 / 6 / 7 は実装され、`tests/test_figure_policy.py` の 12 検査で機械化された。
上の「実装記録」の 10 項目は、次に図を触る人が最初に読むべき記録として質が高い。

本節は、**実装後の図を実際に読んで見つかったもの**を扱う。新しい規約を 3 本足す。

---

## FIG-8: 図が主張することは、その図の条件で検証されていること

FIG-1（タイトルは結論文で書く）を入れた結果、**根拠より先に主張が出る**という新しい失敗が生まれた。
結論文にする以上、**その結論がその図の条件で本当に検証されているか**を書く前に確かめる。

### 実例1: 3-C が Goudarzi の対照を再現していない

`fig_narma10_control.png` の副題は「Goudarzi et al. 2014 の対照 (正則化なし) を第4水準に足した」だが、
実測すると**この設定では過学習が起こりえない**。

| | 本実験 | Goudarzi et al. 2014 |
|---|---|---|
| タップ数 | 最大 30（`config.yaml:142` `n_lags_grid: [10,15,20,25,30]`） | 1,810〜2,000 |
| 訓練長 | 3,800 | 2,000 |
| **タップ数 / 訓練長** | **≒ 0.008** | **≒ 1** |

先行の対照の本質は「正則化なし」ではなく「**正則化なし かつ k ≈ n_train**」である。
公平化されたのは正則化の軸だけで、**タップ数の軸が動いていない**。
「OLS ≒ リッジ（0.1534 vs 0.1538）」は実測として正しいが、**先行の対照設計への批判を検証していない**。

**対応（どちらかを選ぶ。現状の副題のまま残さない）**

- **(a) 記事の芯を成立させる**: `n_lags` を k / n_train が 1 に近づくところまで掃引し、
  OLS が壊れてリッジが壊れない点（あるいは両方壊れる点）を示す。
  `docs/series/rc-basics-survey.md` 未解決3「Goudarzi の DL をリッジ化した追試は誰かやっているか」と接続する
- **(b) 射程を限定する**: 副題と注から「Goudarzi の対照」の主張を外し、
  「小さいタップ数では正則化の有無が結論を変えない」という限定された主張に書き換える

### 実例2: 4-B が参照線なしで「数 Lyapunov 時間」と主張している

`fig_valid_time.png` のタイトルは「自走が真の軌道からずれるまでの時間は**数 Lyapunov 時間**」になったが、
**図の中に文献の有効予測時間の参照線が無い**。4.83 が妥当な水準だという根拠が図に無いまま、
タイトルだけが判断を済ませている。**参照線が無かった初版時点より、主張が先行している分むしろ危うい。**

FIG-2（各記事に文献照合を最低1枚）は 4-B に限らずどの実験でも未実装であり、
`tests/test_figure_policy.py` にも対応する検査が無い。
**FIG-1 / 3 / 4 / 5 / 6 / 7 が機械化された一方で、FIG-2 だけが方針から落ちている。**

**対応**: FIG-2 を実装し、`test_figure_policy.py` に「各実験に文献照合が 1 枚以上ある」検査を足す。
Lorenz の有効予測時間の文献値と条件の特定は、本文書の「未解決」に既に挙げてある（そこが先）。

### 規約

- 結論文タイトルを書く前に、**その結論を否定しうる条件がスイープの外に無いか**を確認する
- 先行研究の再実演・再実験を名乗る図は、**先行の設定のうち何を揃え何を揃えなかったか**を副題か注に書く
- 参照線の無い図で、水準の良し悪しを主張しない（主張するなら FIG-2 を先に満たす）

---

## FIG-9: 図に見える目立った構造は、主張していなくても1行で説明する

`fig_ipc_profile.png`（3-B）は次数1と3に容量があり、**次数2と4は全パネルで真っ暗**である。
入力が対称（一様・平均0）で tanh が奇関数であるため偶数次の容量が消える、という説明が付くはずだが、
図にも注にも無い。

タイトル「rho を上げると次数3の容量が失われ、残るのは次数1になる」は正しい。
しかし読者が最初に目を引かれるのは「**なぜ 2 と 4 だけ空なのか**」であり、そこに答えが無いと
タイトルの主張まで疑われる。

FIG-7 で「未計算」をグレーにして 0 と区別できるようにした以上、**「0 である理由」も 1 行あるべき**である。

**規約**: 図の面積の相当部分を占める構造（空の帯・全面ゼロの行や列・不連続な段差）は、
その図の主張に含まれていなくても注で 1 行説明する。説明できないなら、それは調べるべきことである。

---

## FIG-10: 最終稿前に、全成果物を同一 commit で一斉再生成する

FIG-6 の footnote に commit を焼き込んだ結果、**PNG は生成時の HEAD に依存する**ようになった
（上の「実装記録」§仕様の外で決めたこと 4 が、この帰結を承知のうえで採った判断として記録している）。

その帰結が実測で出ている。

| 図 | footnote の commit |
|---|---|
| `fig_mc_sweep.png`（3-A） | `2ee4aa4` |
| `fig_narma10_control.png`（3-C） | `2ee4aa4` |
| `fig_valid_time.png`（4-B） | `9565eca` |

実験ごとに再生成するので、**記事をまたいで並ぶ図が別バージョンのコードで生成される**。
footnote が正しく仕事をしている証拠でもある（揃っていないことが図から読める）。

D-74 の指紋検査はバイト不変を守るが、「**全成果物が同一 commit で生成されたか**」は見ていない。

**規約**: 連載の最終稿を作る前に `make figures-01..05` を一斉に流し、
全 PNG の footnote の commit が揃っていることを確認する。
この手順を `CLAUDE.md` の「成果物 (`results/`) の扱い」に置く。

---

## 対応表（2026-08-21 再レビュー時点・こちらが正本）

| 図 | 状況 | 残っている対応 |
|---|---|---|
| `fig_esp_map` (2-C) | 型のお手本のまま | 無し |
| `fig_narma10_control` (3-C) | OLS 第4水準を実装済み（D-90）。**射程が Goudarzi の対照に届いていない** | **FIG-8 実例1**。k 掃引か主張の限定 |
| `fig_valid_time` (4-B) | タイトルは結論文になったが**参照線が無い** | **FIG-8 実例2**。FIG-2 + 文献値の特定 |
| `fig_ipc_profile` (3-B) | FIG-7 完了（グレー＋対数軸） | **FIG-9**。偶数次が空である旨の注 1 行 |
| `fig_mc_sweep` (3-A) | FIG-3 / FIG-4 完了（`MC / N` 第2軸 + 縦軸の破断、出典つき） | 無し |
| `fig_memory_nonlinearity` (3-B) | FIG-1 / FIG-5 完了 | 無し |
| `fig_ipc_conservation` (3-B') | FIG-3 完了 | 無し |
| **全図** | FIG-6 完了（footnote + commit） | **FIG-10**。最終稿前の一斉再生成 |
| **全実験** | — | **FIG-2 が全実験で未実装**。`test_figure_policy.py` に検査が無い |

> 上の「実装記録」§実装した範囲の「やらなかったもの」は、3-C の OLS 水準について**1 コミット分古い**。
> D-90（`38412c0`）で実装されている。FIG-2 と 4-B の参照線は未実装のままで正しい。

---

## 未解決（更新）

1. **Lorenz の有効予測時間の文献値と条件**（N・入力次元・観測ノイズ）の特定。
   FIG-2 と FIG-8 実例2 の両方がこれを待っている。**最優先**
2. NARMA10 の参照値 0.16 / 0.107 の原典特定（survey 未解決1）。
   特定できるまでは `[原典未特定]` の表記を外さない
3. **3-C のタップ数をどこまで掃引するか**（FIG-8 実例1 の案 (a) を採る場合）。
   k / n_train ≈ 1 まで伸ばすと確保軸の予算（D-63 系のメモリ上限）に当たる可能性があるため、
   着手前に `experiment/narma.py` の確保点を確認すること
4. 3-B の偶数次が空である理由の確認（FIG-9）。
   対称性による説明で正しいかを 1 度実測で確かめてから注を書く
   （入力分布を非対称にして偶数次が現れるかを見れば足りる）

---
---

# 第3版 — 品揃えと配分（2026-08-22）

*測定: ブランチ `feat/rc-basics-05`、`results/` の PNG 23 枚を実読*
*動機: 「出力が初めての人にピンとこない。論文でよく見る図が端折られている。図のバランスも微妙」*

第2版までは**1枚1枚の作法**（タイトル・出典・色・footnote・打ち切り）を決めてきた。
個々の図の質はこれで上がった（例: `fig_narma10_taps.png` は k/n = 0.91 まで伸ばして
先行の動作点を破線で示しており、図として過不足がない）。

本節が扱うのは**図の品揃えと配分**である。1枚ずつは良いのに、揃えると読めない、という段の話。

---

## 診断: 「どれだけ良いか」の図しか無い

`results/` の PNG 23 枚を型で分類した実測。

| 型 | 枚数 | 例 |
|---|---|---|
| 掃引曲線・ヒートマップ | **14** | `esp_map` / `mc_sweep` / `ipc_profile` / `stability_map` / `narma10_taps` |
| スカラー比較（点と誤差棒だけ） | **5** | `comparison` / `onestep` / `horizon` / `narma10_control` / `valid_time` |
| 状態空間・位相図 | 3 | `state_space` / `freerun_attractor` / `freerun_stats` |
| **時系列（信号そのもの）** | **1** | `05` の `score_timeline` のみ |
| **模式図（仕組みを示す）** | **0** | — |

**「どれだけ良いか」を答える図が 19 枚あるのに、「何をやっているのか」を答える図が実質ない。**

論文・教科書の順序は Fig.1 が模式図、Fig.2 が波形、そこから定量比較に入る。
現在の構成は**いきなり Fig.3 から始まっている**。初めての読者がピンとこない理由も、
研究者が見にくいと感じる理由も、どちらもこれで説明がつく。

---

## FIG-11: 各記事に「仕組みの図」と「信号の図」を1枚ずつ置く

定量比較の図を置く前に、次の2つが記事内に存在すること。

1. **仕組みの図**: その記事で何が動いているかの模式図。連載で1枚を使い回してよい
2. **信号の図**: 入力・目標・予測の時間波形。**その記事で扱う課題の実物**

どちらも「読者が図を見て何を測っているか分かる」ための下地であり、定量比較はその上に載る。

### 追加する図（優先順）

| # | 図 | 置く記事 | 内容 | 理由 |
|---|---|---|---|---|
| 1 | **ESN アーキテクチャ模式図** | 01（第1図） | `u → W_in → リザバー(W, リーク率) → X → W_out → y`。**学習するのは `W_out` だけ**を色で示す。固定部と学習部を `METHOD_COLORS` と別系統の2色で塗り分ける | RC の教科書・レビューには必ず冒頭にある。連載全体で使い回す |
| 2 | **真値 vs 予測の重ね書き** | 01 | Mackey-Glass と遅延パリティについて、テスト区間の一部（200〜400 ステップ）で真値・線形・遅延線・ESN を重ねる | `fig_comparison` は「NRMSE 0.0007」と言うだけで、それがどう見えるかを見せていない |
| 3 | **NARMA10 の真値 vs 予測** | 03 | 線形 / 遅延線 / ESN の3本を重ねる | 「NMSE 0.15 と 0.27 の違い」を目で見せる。3-C の主張の裏づけになる |
| 4 | **自走が外れていく時系列** | 04 | 真値と自走を時間軸で重ね、有効予測時間の点に縦線 | Pathak 2018 Fig.2 の型。4-B には位相図と valid time があるのに**時間軸の図が無い** |
| 5 | **リザバー状態の波形** | 01 または 02 | ニューロン 8 本程度の時間波形 + 入力を上段に | 「高次元に散る」の実体。現状は PCA 散布図だけで、状態そのものを見せていない |

### 模式図の作り方（決定）

**matplotlib で描く。** 手描き SVG は見た目で勝るが、**再生成できないため `artifact_manifest` と
D-74 の規律の外に出る**。コードと乖離しても誰も気づけない状態を、記事の第1図で作らない。

- 置き場所: `plotting/schematic.py`（新規）。`figures_*.py` の凍結行数に触れない
- `style.py` の `new_figure` / `save_png` / `add_provenance` / CJK 切り替えをそのまま使う
- **ラベルの ja / en は `labels.py` の既存規約に従う**（CJK フォントが無い環境で豆腐にしない）
- 図の中の記号（`W_in` / `W` / `W_out` / `a`）は、`docs/design.md` の式で使っている記号と一致させる。
  一致していることを `test_design_doc.py` 系の照合対象に入れられるとなお良い

---

## FIG-12: スカラー比較だけの図を単独で1枚にしない

### 観測

点が少ないのにキャンバスが大きい図がある（実測）。

| 図 | データ点 | 画像サイズ | 問題 |
|---|---|---|---|
| `fig_onestep` | **6 点** | 1551×1062 | y 軸が4桁ぶん。面積の9割が空白 |
| `fig_comparison`（左） | **3 点** | 1711×860 | データは 1e-3〜0.15 なのに軸は 1e-3〜1 超 |
| `fig_horizon` | **5 点** | 1511×1062 | y 軸が3桁ぶん。**凡例が参照線と重なり「このリポジトリ (N = 200)」が読めない** |

6 点なら表のほうが正確に伝わる。図の枠は波形に使うべきである。

### 規約

- **点が10個未満のスカラー比較は、単独の figure にしない。** 時系列・分布・位相図と同じ figure に
  パネルとして同居させる
- 数値そのものを正確に伝えたい場合は、図ではなく `README` / 記事本文の表にする
  （成果物 CSV が既にあるので、表は CSV から生成できる）
- 軸範囲はデータの範囲に合わせる。**参照線を入れるためだけに軸を広げない**
  （参照線が遠いなら、それ自体が「遠い」という主張なので FIG-4 の正規化を検討する）

### 統合の具体案

| 統合 | 空いた枠の使い道 |
|---|---|
| `fig_onestep`（6点）を `fig_freerun_attractor` のパネルへ吸収 | 追加図 4（自走の時系列） |
| `fig_comparison` の2パネルを1パネルに詰め、右に予測波形を並べる | 追加図 2（真値 vs 予測） |
| `fig_horizon` を `fig_comparison` の第3パネルへ | 図が1枚減る |

**足すだけでなく減らすこと。** 現在 23 枚 / 5 記事 = 平均 4.6 枚で、
`docs/series/連載構成案_RC基礎編.md` が想定していた「図 2〜4 枚」を超えている。
情報密度の低い図を残したまま波形を足すと、記事が図で埋まる。

---

## FIG-13 改訂（2026-08-31）: 上限を 3.2 から 2.3 へ締めた

### なぜ

3.2 は「banner の 3 : 1 に余裕を足す」という**体裁だけの根拠**だった。読み手側から
決め直すと別の数字になる —— Zenn の本文幅は概ね 700px なので、比 2.9 の図
（`fig_ipc_profile` は 4 パネル横一列だった）は 1 パネルあたり 165px しか残らず、
記事の中で読めない。

### やったこと（実測）

| 図 | 前 | 後 | 手 |
|---|---|---|---|
| `fig_ipc_profile` | **2.88** | **1.26** | 1×4 → **2×2** に折った（各パネルの幅が倍） |
| `fig_memory_nonlinearity` | 2.79 | 1.95 | 高さを 4.4 → 6.4 |
| `fig_freerun_attractor` | 2.55 | 1.92 | 高さを 5.4 → 7.2 |
| `fig_mc_sweep` | 2.38 | 1.86 | 高さを 4.8 → 6.2 |
| `fig_leak_timescale` | 2.27 | 1.79 | 高さを 4.6 → 5.8 |
| `fig_valid_time` | 2.71 | 1.41 | 脚注の折り返し（FIG-17）。figsize は触っていない |

**全 22 枚の比は 1.13〜2.18 に収まった。** `style.MAX_ASPECT_RATIO` を実測の最大
2.18 に丸め誤差を足した **2.3** に締めた（`tests/test_figure_policy.py` が機械検査）。

### 2 段化は下限も割る

段あたりの高さをそのままに 1×4 を 2×2 へ折ると `fig_ipc_profile` は **0.86 : 1** に
なり、今度は下限 1.0 を割った。段あたりの高さをヒートマップの縦横比に合わせて
1.26 : 1 に直している。**上限だけを見て折ると反対側へ倒れる。**

---

## FIG-13: アスペクト比を3種に限定する

### 観測

**0.87 : 1 〜 4.03 : 1 までばらついている**（実測）。

| 図 | サイズ | 比 |
|---|---|---|
| `fig_score_timeline` | 1831×2113 | 0.87 : 1（縦長） |
| `fig_ipc_conservation` | 1471×1062 | 1.38 : 1 |
| `fig_mc_sweep` | 2431×1022 | 2.38 : 1 |
| `fig_ipc_profile` | 2991×779 | 3.84 : 1 |
| `fig_stability_map` | 3631×901 | 4.03 : 1（横長） |

記事に順に並べたとき、図ごとに幅と高さが変わるので視線のリズムが崩れる。

### 規約

`style.py` に3つの標準サイズを定数で持ち、作図側はそこから選ぶ。

| 用途 | 比 | 想定 |
|---|---|---|
| 単パネル | **4 : 3** | 1課題・1主張の図 |
| 2〜3パネル横並び | **16 : 9** | 比較・掃引 |
| 帯状（多パネル横並び・ヒートマップ列） | **3 : 1** | `ipc_profile` / `stability_map` |

- 3 : 1 を超える場合はパネルを2段に折る（`stability_map` の 4.03 : 1 が該当）
- 縦長（1 : 1 未満）は使わない。`score_timeline` は横に折るか、系列を減らす

---

## FIG-14: 凡例は必ず軸の内側に収める

`fig_horizon` は凡例が3行あり、**参照線と重なって1行目が読めていない**。
凡例テキストに出典（FIG-3）を入れた結果、1行が長くなったことの副作用である。

### 規約

- 凡例が軸の高さの 1/4 を超えるなら、凡例ではなく**注**（footnote の上）に出典を移す
- 参照線が2本以上あり凡例が長くなる図は、線に直接ラベルを置く（`ax.annotate`）ことを検討する
- **`tests/test_figure_policy.py` に「凡例の bbox が軸の bbox に収まっている」検査を足す。**
  重なりは目視でしか見つからないので、機械に置く

---

## 対応表（第3版・追加分）

| 図 | 対応 |
|---|---|
| **新規: `fig_architecture`** | FIG-11 追加図1。`plotting/schematic.py` に matplotlib で |
| **新規: 予測波形（01 / 03 / 04）** | FIG-11 追加図 2 / 3 / 4 |
| **新規: リザバー状態の波形** | FIG-11 追加図5 |
| `fig_onestep` | FIG-12。`freerun_attractor` へ吸収して単独図をやめる |
| `fig_comparison` | FIG-12。2パネル→1パネル + 予測波形。`fig_horizon` も第3パネルへ |
| `fig_horizon` | FIG-14（凡例の重なり）→ そのうえで FIG-12 で統合 |
| `fig_stability_map` | FIG-13。4.03 : 1 を2段に折る |
| `fig_score_timeline` | FIG-13。縦長をやめる |
| 全図 | FIG-13 の標準サイズを `style.py` に定数化して置き換え |

---

## 実装の順序（提案）

1. **FIG-14 の検査を先に足す**（凡例の bbox）。既存 23 枚のうち何枚が引っかかるかが分かる
2. **FIG-13 の標準サイズを `style.py` に定数化**し、既存図を置き換える。振る舞いは変わらないが
   **全 PNG のバイトが変わる**ので、D-74 の「意図した変更」経路を通す（`make artifacts-manifest` +
   コミットメッセージに理由）。FIG-10 の一斉再生成と同じタイミングでやると1回で済む
3. **模式図（追加図1）を作る。** これだけは他の図と独立していて、既存成果物を1バイトも変えない
4. **予測波形（追加図 2 / 3 / 4）を作り、同時に FIG-12 の統合を行う。** 足すと減らすを一度にやる
5. リザバー状態の波形（追加図5）

3 を先に単独で出せるので、**見た目の型についてはそこで一度合意を取る**とよい。

---

## 未解決（第3版で追加）

5. **模式図に何を書き、何を書かないか。** リーク率・washout・リッジの正則化まで図に入れると
   密度が上がりすぎる。「固定 / 学習」の二色分けだけを主役にして、残りは記事本文へ逃がす、
   という方針でよいかを模式図の初稿で確認する
6. **予測波形をどのテスト区間から切り出すか。** 恣意的に「よく当たっている区間」を選ぶと
   図が主張を作ってしまう（仕様 §5 の禁止する構造）。**区間の選び方を決定として記録する**
   （例: テスト区間の先頭から固定オフセット、レプリケート 0 固定）
7. リザバー状態の波形で**どのニューロンを見せるか**。同じ理由で、選び方を決定として記録する
   （例: `W_in` の重みが大きい順に 8 本、ではなく単純にインデックス 0〜7）

---
---

# 第4版 — 追加図の実装レビュー（2026-08-22）

*測定: ブランチ `feat/rc-basics-05`、HEAD `43313a7`。`results/` の PNG 24 枚を実読*
*比較の基準: 第3版の測定時点（PNG 23 枚、HEAD `38412c0`）*

第3版の FIG-11〜FIG-14 に対する実装のレビュー。

---

## V. 効いた分（実測）

| 項目 | 第3版時点 | 現在 |
|---|---|---|
| アスペクト比の幅 | 0.87 – 4.03 : 1 | **1.19 – 2.93 : 1**（`stability_map` 4.03 → 1.69 は2段に折った） |
| 時系列の図 | 1 枚 | **4 枚**（`narma10_waveform` / `freerun_timeline` / `state_waveform` / `comparison` 内2パネル） |
| スカラー単独図 | `onestep` / `horizon` | **どちらも廃止**、パネルへ畳んだ |
| FIG-13 / FIG-14 の機械化 | 無し | `test_figure_policy.py`（凡例はみ出し・アスペクト比・例外リストの陳腐化検出・凡例の不透明化まで） |
| 波形の区間・ユニット選択 | 未解決 6 / 7 | **固定として D-107 に記録**。注にも「よく当たっている区間を選べる図にしない」と明記 |

`fig_freerun_timeline` は Pathak 型そのもので、有効予測時間の縦線が入り、外れた後も振幅が保たれることが一目で読める。第3版 追加図4 の狙いをそのまま満たしている。
`fig_state_waveform` の「入力を上段・状態を下段」の構成も正しい。

---

## W. 指摘 C-1: FIG-1 だけ機械検査が無く、新規図でそこが破れた

**本節でもっとも優先度が高い。**

### 観測

図の規約のうち、機械検査があるものと無いものを並べる。

| 規約 | 検査 |
|---|---|
| FIG-2 文献照合 / FIG-3 出典 / FIG-4 正規化 / FIG-5 色 / FIG-6 footnote / FIG-7 打ち切り | あり |
| FIG-11 波形の選択 / FIG-12 スカラー単独禁止 / FIG-13 アスペクト比 / FIG-14 凡例 | あり |
| **FIG-1 結論文タイトル** | **無し** |

そして今回追加された 3 枚のうち 2 枚が疑問形になっている。

```
src/rc_basics_lab/plotting/figures_freerun_time.py:111
    "実験 4-B: {task_label[0]} の自走はどう外れていくか"
src/rc_basics_lab/plotting/waveforms.py:192
    "{task_label[0]}: 予測がどう見えるか"
```

既存 19 枚は結論文なのに、**検査が無い 1 本だけが新規追加で崩れた**。

### 診断

このリポジトリが繰り返し確認してきた「機械に置いた規約は守られ、文書だけの規約は次に破れる」の、図の側での再現である。
`docs/process/agent-system-review-from-artifacts.md` 第3版 U 節の一般則がそのまま当てはまる。

`fig_freerun_timeline` は特にもったいない。**あの図が実際に示しているのは
「約 6 Lyapunov 時間で位相はずれるが、振幅とアトラクタの形は保たれる」という 4-B の主張そのもの**である。
タイトルがそれを言えば、図と主張が直結する。

### 対応

**タイトル末尾が「か」「?」で終わることを禁じる検査を `test_figure_policy.py` に足す。**
粗い判定だが、今回の 2 件はこれで落ちる。完全な「結論文か」の判定は機械にできないので、
**疑問形の禁止という下限だけを機械に置く**。

---

## X. 指摘 C-2: 波形図の凡例が `METHOD_LABELS` を通っていない

### 観測

`src/rc_basics_lab/plotting/waveforms.py:148` が `label=name` で生のキーを渡している。
他の作図はすべて `labels.METHOD_LABELS` を経由している。

結果、**`fig_comparison` の中で同じものが 2 通りに呼ばれている**。

| 場所 | 表記 |
|---|---|
| 左上パネルの x 軸ラベル | 線形 / 遅延線 (リッジ) / ESN |
| 右上・左下パネルの凡例 | `linear` / `delay_line` / `esn` |

さらに `style.label(ja, en)` を通していないため、**CJK フォントが無い環境での英語フォールバックが効かない**。
`labels.py` の冒頭が「フォントだけ落とすと豆腐の図が正常に生成されてしまい、生成の成否では検出できない
壊れ方をする」と書いて避けた経路が、ここだけ素通りしている。

### 対応

- `waveforms.py` の凡例を `METHOD_LABELS` + `style.label` 経由にする
- FIG-5 に条項を足す: **色だけでなく手法名も連載通しで固定する。作図側で生のキーを凡例に出さない**
- 機械検査: 凡例テキストに `METHOD_LABELS` のキー（`linear` / `delay_line` / `esn` …）が
  そのまま現れていないことを `test_figure_policy.py` で見る

---

## Y. 指摘 C-3: 「予測がどう見えるか」の図で差が見えていない

### 観測

`fig_comparison` 右上（Mackey-Glass 1ステップ先予測）は 4 本が完全に重なり、1 本にしか見えない。
同じ図の左上パネルは「線形 0.1454 vs ESN 0.0007」と 200 倍の差を主張しているが、
**その差が波形のどこに出ているのかが読めない**。

`fig_narma10_waveform` も同様で、こちらはより危ない。残差が無いので目視では ESN（緑）と
遅延線（青）の優劣が判断できず、**3-C の「遅延線 0.1538 < ESN 0.2673」という結論と
読者の印象が食い違いうる**。

### 診断

波形パネルを足した目的は「定量比較の裏づけを見せる」ことだった（第3版 FIG-11 追加図 2 / 3）。
**残差が無いと、その目的が半分しか果たされない。** 「全部合っている」しか伝わらない。

論文の定番は上段に真値と予測、下段に残差（真値 − 予測）を同じ x 軸で並べる形である。

### 対応

**FIG-11 に条項を足す**: 予測波形の図は、真値と予測を重ねた段の下に**残差の段を必ず置く**。
残差の y 軸は手法間で共通にする（手法ごとにスケールを変えると差が消える）。

---

## Z. 指摘 C-4: 波形の区間が長すぎて線が判別できない

| 図 | 区間 | 問題 |
|---|---|---|
| `fig_narma10_waveform` | 300 ステップ | NARMA10 は高周波。4 本重ねるとスパゲッティになる。**100 ステップ程度に絞る** |
| `fig_comparison` 左下（遅延パリティ） | 80 ステップ | ±1 の矩形波が 20 周期以上。真値と ESN が重なって判別しづらい。**40 ステップ程度に** |

区間長は `waveforms.py::waveform_steps_for(task)` が課題ごとに持っているので、値の変更だけで済む。

**区間の開始位置は固定のまま変えないこと**（D-107）。短くするのは「読めるようにする」ためであって、
「よく当たっている区間を選ぶ」ためではない。**開始位置は 0 のまま、長さだけを縮める。**

---

## AA. 指摘 C-5: FIG-9（3-B の偶数次）が未実装のまま

`figures_capacity.py` に「偶数」「奇関数」「対称」のいずれも現れない。
第2版 P 節の指摘が 2 巡残っている。

第2版の未解決 4 が「対称性による説明で正しいかを 1 度実測で確かめてから注を書く」としたため、
確認待ちで止まっていると見られる。**実測は入力分布を非対称にして偶数次が現れるかを見るだけ**なので、
そこを先に片づける。

---

## AB. 指摘 C-6: 記事あたりの図が減っていない

第3版 FIG-12 は「足すだけでなく減らす」だったが、正味では **23 → 24 枚**（−2 枚 +3 枚）。

| 記事 | 枚数 | `連載構成案_RC基礎編.md` の想定 |
|---|---|---|
| 01 | 3 | 2〜3 ✓ |
| 02 | 4 | 3〜4 ✓ |
| **03** | **7** | 2〜4 ✗ |
| 04 | 5 | ✗ |
| 05 | 5 | ✗ |

03 が想定の倍近くある。`narma10_control` / `narma10_taps` / `narma10_waveform` の 3 枚は
**同じ主張（遅延線と ESN の関係、正則化とタップ数の効き）を支えている**ので、
パネルで 1〜2 枚に畳める。

FIG-12 に条項を足す: **1 記事あたりの図は最大 4 枚**。超えたら統合する。
`test_figure_policy.py` で `results/0N_*/` の PNG 枚数を数えれば機械で見られる
（FIG-13 の例外リストと同じ形で、既存の超過分は凍結して増やせなくする）。

---

## AC. 記録: アーキテクチャ模式図を成果物から外した判断について

`22bedf3` で「アーキテクチャ図はユーザーが自前で作るか文献から取る」として revert された。
**判断そのものは尊重する**（記事の第1図をどう用意するかは記事側の裁量である）。

ただし記録として 1 点だけ整合していない。revert の理由は

> 自動生成版を残すと再生成のたびに作られ、指紋にも載り続けるので外した

だが、追加時のコミット `7e40e77` は**まさにその点を利点として**挙げていた。

> results/ に置けば D-74 の指紋検査と FIG-10 の一斉再生成の対象に入る

同じ性質を、一方は利点、他方は欠点として書いている。後から読むと判断の根拠が追えない。

**対応**: どちらが正しいかではなく、**外した本当の理由**（見た目が記事の水準に届かない／
外部から持ってくる当てがある／記事構成の判断は実装者がしない、など）を 1 行に直す。
そのうえで、記事 01 に模式図が無い状態が残るので、以下を未解決に置く。

- 誰がいつ用意するか
- どこに置くか（`results/` の外に置くなら指紋検査の対象外になることを承知で）
- コードと乖離したときにどう気づくか

---

## 対応表（第4版・追加分）

| 対象 | 対応 | 労力 |
|---|---|---|
| `figures_freerun_time.py:111` / `waveforms.py:192` | **C-1**。結論文へ。4-B は「約 6 Lyapunov 時間で位相はずれるが振幅とアトラクタの形は保たれる」の線 | 小 |
| `test_figure_policy.py` | **C-1**。タイトル末尾の「か」「?」を禁じる検査 | 小 |
| `waveforms.py:148` | **C-2**。`METHOD_LABELS` + `style.label` 経由へ | 小 |
| `test_figure_policy.py` | **C-2**。凡例に生のキーが出ていないことの検査 | 小 |
| `fig_comparison` / `fig_narma10_waveform` | **C-3**。残差の段を追加 | 中。**図の価値が一番上がる** |
| `waveforms.py::waveform_steps_for` | **C-4**。narma10 を 100、delay_parity を 40 程度へ | 小（定数） |
| `figures_capacity.py` | **C-5**。偶数次が空である旨の注（実測確認が先） | 小 |
| 03 の 3 枚 | **C-6**。統合して記事あたり 4 枚以内に | 中 |
| `22bedf3` の理由 | **AC**。理由を直し、模式図の当てを未解決に残す | 小 |

**C-1 / C-2 / C-4 はいずれも小さいので 1 コミットにまとまる。**
図の質という意味では **C-3 が最も効く** —— 波形を足した目的が「定量比較の裏づけを見せる」ことなので、
残差が無いと足した意味が半分になる。

---

## FIG-16: 同じ量の軸は共有し、ラベルは外周だけに出す（2026-08-31、1-7）

多パネル図で同じ量の軸が並んでいるのに `sharex` / `sharey` を使わないと、
同じ軸ラベルがパネルの数だけ繰り返される。実測（2026-08-30）:

| 図 | 繰り返し | 直した形 |
|---|---|---|
| `fig_ipc_profile` (3-B) | 「遅延 k [ステップ・対数]」が 4 回 | `sharex` / `sharey` + x ラベルは最下段だけ |
| `fig_memory_nonlinearity` (3-B) | 「スペクトル半径 rho」が 3 回 | 1 行なので `supxlabel` に1つだけ |
| `fig_protocol_sensitivity` (5-C) | 27 個の x 目盛りラベルが 2 回 | **横並びをやめて縦に積む** + `sharex` |
| `fig_score_timeline` (5-A) | 拡大列の y 目盛り | `sharey` + 拡大側の目盛りラベルを消す |

**1 行しか無い図では `sharex` は効かない**（全パネルが最下段にあたる）。
その場合は各パネルの `set_xlabel` を消して `figure.supxlabel` に寄せる。

5-C は目盛りラベルの重複だけの問題ではなかった。2 パネルが同じ 27 個の格子点を
横軸に持つのに横並びだったので、「この格子点で順位が動き、逆転が何組あったか」を
27 箇所ぶん目で突き合わせることになっていた。**縦に積むと同じ x 位置で縦に読める。**

---

## FIG-15: パネル予算 —— 枚数だけでは実態を捉えていない（T6）

### 問題

図の枚数について**2つの文書が違うことを言っていた**。

| 文書 | 基準 |
|---|---|
| `writing_article/README.md` | 図は **3〜4 枚まで** |
| `docs/series/連載構成案_RC基礎編.md` | 01 は 2〜3、04 は 4〜5、**05 は 5** |

そのため記事 03・04・05 は毎回「README を超えている」と素案メモに書かれていた。
さらに**枚数だけでは実態を捉えていない** —— 記事01 は 3 枚だが、`fig_comparison` が
6 パネル、`fig_state_space` が 6 パネルあり、読者が向き合うのは 14 パネルである。
「枚数を減らす」と言いながらパネルへ畳んだだけなら、読者の負荷は下がっていない。

### 実測（2026-08-31、`make panels`）

`Figure.savefig` を捕まえて数えた。**本番設定で実際に生成される図そのもの**を
数えている（コードの読み取りではない）。数えるのは**パネル記号 `(a)` が振られた
軸**で、`len(figure.axes)` ではない（下の節を参照）。参考のため軸の本数も出す。

| 記事 | 枚数 | 総パネル | 1枚の最大 | 軸の本数 | 内訳（パネル） |
|---|---|---|---|---|---|
| 01 | 3 | 12 | 6 | 14 | comparison 4 / state_space 6 / state_waveform 2 |
| 02 | 4 | 8 | 3 | 9 | esp_decay 1 / leak_timescale 3 / esp_map 2 / washout 2 |
| 03 | 5 | **13** | 4 | 16 | mc_sweep 2 / ipc_profile 4 / memory_nonlinearity 3 / ipc_conservation 1 / narma10 3 |
| 04 | 5 | **14** | 5 | 14 | freerun_attractor 3 / valid_time 1 / freerun_timeline 1 / stability_map 5 / freerun_stats 4 |
| 05 | 5 | 12 | 6 | 18 | pr_curves 1 / score_timeline 6 / threshold_tradeoff 2 / protocol_sensitivity 2 / size_vs_performance 1 |

**2026-08-30 の実測から減った**のは、数え方を軸の本数からパネル記号へ変えたため
である（01: 14 → 12、04: 15 → 14）。03 は `fig_narma10` に記号を振ったので
逆に増えた（記号が無い図はそれまで軸の本数で数えられていた）。02 は
`fig_leak_timescale` に比のパネルを足し、`fig_washout_sensitivity` に記号を
振ったぶん増えている。

### 決めたこと

- **1記事あたり総パネル 12 以内**（`FIG-12` の枚数上限 4 と併用する）
- **1枚あたり 6 パネルまで**
- 模式図は別枠（`results/` に置かない、AC の判断）

**上限を実測の最大値に置いた**のは意図的である。当初案は「総 12 / 1枚 4」だったが、
実測すると 5 記事中 4 記事が総パネルを、4 枚が 1 枚あたりを超えており、そのまま
規則にすると**全部が即違反**になる。守れない上限は守られないので上限そのものが
無効になる（`FIGURE_COUNT_FROZEN` を置いたときと同じ判断）。ここでは
**「今より増やさない」**を確実にし、削減は記事構成の判断として別途行う。

超過中の 03（13）・04（14）は現在値で凍結する。**減らすのは自由で、減らしたら
この表を更新する。** 01 は 14 → 12 になり上限内に戻った（数え方を直した結果で、
図を減らしたわけではない）。

### 数えるのは「読者が数えるパネル」（2026-08-31 追記）

`len(figure.axes)` は matplotlib の軸の本数であって、読者が数えるパネルの数では
ない。次の 2 つは**1 パネルとして数える**:

- **破断した軸の上下**（`fig_mc_sweep` の左）—— 上段は上限線だけを載せた
  同じ 1 枚の続きで、別の主張はしていない。
- **全区間パネルとその拡大**（`fig_score_timeline` の右列）—— 同じ系統の同じ
  量を、倍率を変えて 2 回描いている。

判定はパネル記号で行う。**記号 (a)(b)(c)… が付いている軸だけを数える。**
記号を付ける対象は「読者が独立に読む単位」なので、この基準は図の側で既に
決まっており、数える側で解釈を足す必要がない。`make panels` が記号を数えるので、
上の表は機械の出力そのものである。

**記号が1つも無い図は軸の本数へ落とす。** 最初これを「1」にしたところ、記号を
振り忘れた `fig_narma10`（4 軸）が 1 パネルと報告された。パネル予算が黙って
小さくなるのはラチェットとして最悪の壊れ方である。この実測が記号の振り忘れ
2 枚（`fig_washout_sensitivity` / `fig_narma10`）を炙り出した。

### 機械化していない理由

パネル数は図を実際に描かないと数えられず、本番設定の全実験を回すと約 20 分かかる。
`make ci` に入れると CI が実験を回すことになり、「CI は実験を回さない」という
既存の分担が壊れる。**`make panels` として手動実行にし、この表を一次資料とする。**
枚数のほう（`FIG-12`）は PNG を数えるだけなので `tests/test_figure_policy.py` が
機械で守っている。**この節はその機械化の外にあることを、ここに明記しておく。**

### FIG-1 への追加条項（T2 から）

**結論が定義に依存する場合は、定義をタイトルに畳む。**

実例: 2-B の見出しは「リーク率を下げると時定数は理論の約 1.4 倍で平行に伸びる」
だったが、比 1.4 が成り立つのは `tau_1e` の定義のときだけである。もう一方の
定義 `tau_integrated` は理論との比が 1.58 → 1.94 とリーク率とともに開く
（実測は `docs/design.md` §9.2）。図は `tau_1e` しか描いていないので、定義を
書かないと読者はどちらの話か判定できない。「実験 2-B: **1/e 時定数**はリーク率に
よらず理論の約 1.3〜1.4 倍」へ直した（D-117）。

---

## 未解決（第4版で更新）

4. ~~**3-B の偶数次が空である理由の確認**~~ → **解決（T3 / D-116）**。
   実験 3-S（`make symmetry-03`）で実測した。分布の形は変えず**平均だけ**ずらす
   （非対称な分布に替えると Legendre 基底が正規直交でなくなり容量が二重計上される）。
   偶数次の割合は offset 0 で 0.2%、0.5 sigma_u で 32.4%、3 sigma_u で 55.3%。
   数値は `docs/design.md` §11.6
5. **記事 01 の模式図をどう用意するか**（AC）。誰が・どこに・乖離をどう検出するか
8. **残差パネルの y 軸をどう揃えるか**（C-3）。手法間で共通にするのは決まりだが、
   課題間（Mackey-Glass と遅延パリティ）でも揃えるか、課題ごとに切るかは未定
9. **03 の 3 枚をどう畳むか**（C-6）。`narma10_control`（4 水準の点）と `narma10_taps`（k 掃引）は
   x 軸が違うので単純な横並びにはならない。記事構成の判断が要る
