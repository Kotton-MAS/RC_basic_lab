# 修正指示 — 記事整合レビュー (2026-08-30) の反映

*根拠: `docs/series/記事整合レビュー_2026-08-30.md`(全文・実測値つき)*
*対象リポジトリ: `RC_basic_lab`(本リポジトリ)。記事側 `writing_article` の文面修正は 2026-08-30 に別途適用済み*

---

## 読む順番

T0 → T1 は**依存関係があるので順番を守ること**。T2 以降は独立に着手してよい。
各タスクは「受け入れ条件」を満たしたら完了。満たせない場合は完了にせず、理由を書いて止める。

| ID | 内容 | 優先 | 追加実験 |
|---|---|---|---|
| T0 | ブランチを統合して「記事の正本」を1つにする | **最高** | なし |
| T1 | `main` で全実験を再生成し、記事の図と一致させる | **最高** | 再実行のみ |
| T2 | 図 2-B のタイトルを τ の定義に合わせる | 高 | なし |
| T3 | IPC の偶数次が空である理由を実測で確かめる | 中 | **あり** |
| T4 | 「ほとんど動かないユニット」の割合を測る | 低 | **あり** |
| T5 | docs/ 整理の残り(マージ後に流入する文書の再配置) | 中 | なし |
| T6 | 図の枚数ルールの二重化を解消する | 中 | なし |
| T7 | 記事05が MGAB 単独である事実を設定側にも固定する | 中 | なし |

---

## T0: ブランチを統合して「記事の正本」を1つにする

### 現状(2026-08-30 実測)

```
main                      8497775   (= origin/main)
fix/readme-runtime-drift  8327d69   main + 44   ← 記事の図・数値の実体はここ
feat/rc-basics-lab        dd21129   main から 339 遅れ / 独自 7 コミット  ← 現在チェックアウト中
feat/rc-basics-05         6c84e51
feat/agent-fixes-ab       (main にマージ済み)
```

- `.claude/worktrees/rc-basics-05/` は `git worktree list` が **prunable** と報告する
  (管理情報が壊れていて、中で git コマンドが動かない)。にもかかわらず、
  **記事 01〜05 が使っている `results/` と画像はこの worktree のもの**である
- `feat/rc-basics-lab`(現在のツリー)には `experiment/horizon.py` /
  `experiment/state_waveform.py` / `plotting/figures_horizon.py` が**存在しない**。
  `main` にも無い。あるのは `fix/readme-runtime-drift` だけ
- したがって **記事01の図1右下パネル(自走 84 ステップ)と図3(状態波形)は、
  公開リポジトリの `main` を clone しても再生成できない**

### やること

1. `fix/readme-runtime-drift` を `main` へマージする(PR 経由)
2. `feat/rc-basics-lab` の独自 7 コミット(モジュール行数テスト・docstring 規約・
   `_csv.write_rows` への集約・ゴールデンテスト)を `main` へ取り込む。
   コンフリクトが大きい場合は、7 コミットの**意図だけ**を新しいブランチで再実装してよい
3. `.claude/worktrees/rc-basics-05/` の中身のうち `main` に無いものが無いことを確認してから、
   ディレクトリごと退避する(`git worktree prune` は管理情報しか消さない)
4. 以後、**記事の数値の正本は `origin/main` とする**

### 受け入れ条件

- [ ] `origin/main` に `experiment/horizon.py` と `experiment/state_waveform.py` がある
- [ ] `origin/main` で `make ci` が green
- [ ] `git worktree list` に prunable な行が無い
- [ ] `feat/rc-basics-lab` の 7 コミットぶんのテスト(`tests/test_module_size.py`,
      `tests/test_golden.py` など)が `origin/main` で走っている

---

## T1: `main` で全実験を再生成し、記事の図と一致させる

T0 の完了が前提。

### やること

1. `origin/main` の clean な作業ツリーで全実験を回す(01 / 02 / 03 / 04 / 05)
2. 生成された `results/**/*.png` を `writing_article/images/<slug>/` へ差し替える
3. 記事本文の数値を、再生成後の CSV と**もう一度**照合する

### 照合すべき値(2026-08-30 時点では全一致している。再生成後にズレたら記事を直す)

| 記事 | 値 | 正本 |
|---|---|---|
| 01 | 0.0005283 / 0.0007106 / 0.08942 / 1.0004 / 1.0007 | `results/comparison_summary.csv` |
| 01 | 符号正解率 0.500 / 0.522 / 1.000 | 同上 |
| 01 | 遅延埋め込み 62 主成分 / リザバー状態 4 主成分 | `results/meta.json` の `state_space` |
| 01 | 自走 84 ステップ log10 平均 −2.1216 | `results/horizon.csv` |
| 01 | 文献 −4.20 / 従来手法 −1.35 | `plotting/figures_horizon.py` の定数 |
| 02 | 境界の ρ: σ_u=0→1.0, 0.05→1.1, 0.1→1.1, 0.2→1.3, 0.5→1.5, 1.0→1.7, 2.0→なし | `results/02_esp_and_dynamics/esp_threshold_sensitivity.csv` |
| 02 | しきい値感度 `max_abs_shift = 0.0`(9条件すべて) | 同上 |
| 02 | washout 感度 6組すべて 最大/最小 ≤ 1.012 | `washout_sensitivity.csv` |
| 02 | τ_1e/理論 = 1.41 / 1.41 / 1.32 / 1.34 / 1.34 | `esp_diagnostics.csv`(2B_timescale) |
| 03 | NMSE 1.0181 / 0.1538 / 0.1534 / 0.2673 | `results/03_capacity/narma10.csv` |
| 03 | MC_total 最大 37.84(= 上限 200 の 18.9%) | `results/03_capacity/capacity.csv` |
| 04 | 有効予測時間 0.067 / 0.252 / 4.734 | `results/04_chaotic_freerun/freerun.csv` |
| 04 | 3態: ノイズ 0/1e-4/1e-3/1e-2 で attractor 49/52/55/47, diverged 31/28/24/32, periodic 0/0/1/1 | `results/04_chaotic_freerun/stability.csv` |
| 05 | AUPRC 0.1602 / 0.0570 / 0.0559 / 0.0560 / 0.0558、対照 0.0567 | `results/05_anomaly_detection/anomaly.csv` |

**`±` はすべて母標準偏差 (ddof=0)。** 標本標準偏差に変えると 04 の ESN が 1.019 → 1.074 になり、
記事と食い違う。集計側の定義を変更する場合は記事も同時に直すこと。

### 受け入れ条件

- [ ] `writing_article/images/` の 22 枚が `origin/main` の1コマンド再生成で作れる
- [ ] 上表の値が再生成後の CSV と一致する(1つでもズレたら記事側を直してから完了)

---

## T2: 図 2-B のタイトルを τ の定義に合わせる

### 問題

`fig_leak_timescale.png` のタイトルは
「実験2-B: リーク率を下げると時定数は理論の約 1.4 倍で平行に伸びる」だが、実測は τ の定義で変わる。

| リーク率 a | τ_1e / 理論 | τ_integrated / 理論 |
|---|---|---|
| 0.1 | 1.41 | 1.58 |
| 0.2 | 1.41 | 1.55 |
| 0.3 | 1.32 | 1.59 |
| 0.5 | 1.34 | 1.72 |
| 0.7 | 1.34 | 1.94 |

「約 1.4 倍で平行」が成り立つのは **τ_1e のほうだけ**。`τ_integrated` はリーク率とともに比が開く。

### やること

- 図のタイトル・軸ラベルに**どちらの τ を描いているかを明記**する
  (例: `実験2-B: 1/e 時定数はリーク率によらず理論の約 1.3〜1.4 倍`)
- 両方を描いているなら、系列名で τ_1e / τ_integrated を区別する
- `docs/series/図の設計方針_RC基礎編.md` の FIG-1(タイトルは結論文)に沿って、
  結論が定義に依存する場合は**定義をタイトルに畳む**、という指針を追記する

### 受け入れ条件

- [ ] 図のタイトルから、比 1.4 がどちらの τ の話か読者が判定できる
- [ ] `tests/test_plotting_style.py` 系のラベル検査が green

---

## T3: IPC の偶数次が空である理由を実測で確かめる

### 問題

記事03 §2.1 で「次数2と4が全パネルで空」の理由を書きたいが、**未検証**。
仮説は「入力が対称(一様・平均0)で tanh が奇関数なので偶数次の容量が消える」。
連載の規律(未確認のまま書かない)により、確かめるまで本文に書けない。

### やること

- 入力分布を**非対称**にした条件を1つ追加して IPC を測り、偶数次の容量が現れるかを見る
- 現れれば仮説が支持される。現れなければ別の理由(直交基底の構成・打ち切り)を疑う
- 結果は `docs/design.md` §11 に数値付きで記録し、記事にはその要約だけ書く

### 受け入れ条件

- [ ] 対称入力と非対称入力の IPC プロファイルが同じ図または同じ CSV で比較できる
- [ ] 偶数次の容量の有無が**行の値**で判定でき、図の目視に依存しない
- [ ] `docs/design.md` に実測値が入っている

---

## T4: 「ほとんど動かないユニット」の割合を測る

### 問題

記事01 §3.3 に「ほとんど動かないユニットが混ざっている」と書いてあるが、
**`results/` に該当する量が無い**。印象で書いている状態。

### やること

- 状態行列 `X` の**ユニットごとの分散の分位数**(あるいは分散が中央値の 1/100 未満のユニット数)を
  `meta.json` に出す。診断層は `X` だけを入力に取る規律を守ること(ESN に依存させない)
- 記事はその数値を引用する

### 受け入れ条件

- [ ] `results/meta.json` にユニット活性度の要約が入っている
- [ ] 記事01 §3.3 の TODO がその数値で埋められる状態になっている

---

## T5: docs/ 整理の残り

### 2026-08-30 に実施済み(この作業ツリー上)

```
docs/
├── README.md                 ← 新規。歩き方・動かしてはいけないファイル
├── design.md                 ← 動かさない (tests/test_design_doc.py が固定)
├── review-findings-01..03b.md ← 動かさない (tests/test_finding_id_references_resolve.py が固定)
├── adr/                      ← 変更なし
├── plans/                    ← 変更なし (このファイルを追加)
├── series/                   ← 新規: 連載構成案・要件 01〜05・図の設計方針・survey・整合レビュー
└── process/                  ← 新規: エージェント運用の振り返り・システムレビュー・リファクタリング方針
```

`docs/` を参照していた文書内のパス文字列(`docs/連載構成案_RC基礎編.md` → `docs/series/...` など)は
一括で書き換え済み。書き換えたのは `docs/review-findings-01.md` / `docs/plans/rc-basics-02..04.md` /
`docs/series/図の設計方針_RC基礎編.md` / `docs/process/*.md` の10ファイル。

**この整理は `feat/rc-basics-lab` の作業ツリーで行っており、pytest は未実行**
(この環境の `.venv` が別プラットフォーム向けで動かせなかった)。

### やること

1. **まず `uv run pytest -q` を回して green を確認する**。落ちたら、落ちたテストが
   `docs/` のパスを参照していないかを見る
2. T0 のマージ後、`main` 側から流入する次の文書を同じ規則で再配置する:

   | ファイル | 行き先 |
   |---|---|
   | `docs/agent-behaviour-fixes.md` | `docs/process/` |
   | `docs/kit-fixes-for-another-session.md` | `docs/process/` |
   | `docs/checkpoint-05b-t3.md` | `docs/process/`(作業チェックポイントなら `.claude/tmp/` でもよい) |
   | `docs/next-pr-candidates-05a.md` | `docs/process/` |
   | `docs/チェックリセット_05.md` | 中身を見て `docs/process/` か `.claude/tmp/` |
   | `docs/…削減候補-05.md` | `docs/process/` |
   | `docs/…含めない.md` | 中身を見て判断。方針文書なら `docs/process/` |
   | `docs/plans/rc-basics-05.md` | `docs/plans/` のまま |

3. `docs/README.md` の表を、再配置後の実態に合わせて更新する
4. (任意) `design.md` と `review-findings-*` も `docs/records/` などへ動かしたい場合は、
   `tests/test_design_doc.py:37` の `DESIGN` と
   `tests/test_finding_id_references_resolve.py:43-44,126` を**同じコミットで**直す

### 受け入れ条件

- [ ] `uv run pytest -q` が green
- [ ] `docs/` 直下の `.md` が `README.md` / `design.md` / `review-findings-*.md` だけになっている
- [ ] `grep -rn "docs/" src tests` の結果に、存在しないパスが無い

---

## T6: 図の枚数ルールの二重化を解消する

### 問題

- `writing_article/README.md` の自ルール: **「図は3〜4枚まで」**
- `docs/series/連載構成案_RC基礎編.md` の目安: 01 は 2〜3、04 は 4〜5、**05 は 5**

食い違っているため、記事 03・04・05 が毎回「README を超えている」と素案メモに書かれる状態。
さらに枚数だけでは実態を捉えていない —— 記事01 は3枚だが、
図1が4パネル(+残差2)、図2が6パネルで**実質13パネル**ある。

### やること

- `docs/series/図の設計方針_RC基礎編.md` に**パネル予算**の節を追加する
  (提案: 1記事あたり総パネル 12 以内、1枚あたり 4 パネルまで、模式図は別枠)
- `連載構成案_RC基礎編.md` の枚数目安を、パネル予算と矛盾しない形に直す
- `writing_article/README.md` の「3〜4枚まで」を、決めた基準に合わせて書き換える
  (このリポジトリからは直せないので、**指示だけ出して人間に渡す**)

### 受け入れ条件

- [ ] 記事 01〜05 の現状のパネル数を数えた表が図の設計方針にある
- [ ] 2つの文書の基準が矛盾していない

---

## T7: 記事05が MGAB 単独である事実を設定側にも固定する

### 背景

記事05 の本文は当初「UCR Anomaly Archive と MGAB を使った」と書いていたが、
`results/05_anomaly_detection/meta.json` の `dataset.source` は `mgab` 単独、
`anomaly.csv` は 90 行すべて MGAB で、**UCR は1点も使っていなかった**。
記事側は 2026-08-30 に MGAB 単独の実験として書き直し済み(タイトルの「実データ」も削除)。

### やること

1. `.claude/decisions.yaml` に判断として記録する。案:
   - 決定: **記事05 の本番設定は MGAB 単独とし、UCR は回さない**
   - 理由: UCR はライセンス未指定で再配布方針が決まらない。かつ系列ごとに前処理を
     切り替える必要があり、「全手法で前処理を共通化する」(D-57)という 5-A の前提と衝突する
   - guard_test: `experiments/05_anomaly_detection/config.yaml` の `dataset.source` が
     `mgab` であること、および `results/05_anomaly_detection/meta.json` の
     `dataset.source` が `mgab` であることを検査する
2. `README.md` の「データセットのライセンスと取得手順」節に、
   **UCR は取得コードのみで本番設定では使っていない**ことを明記する
3. `src/rc_basics_lab/datasets/ucr.py` の冒頭 docstring に、
   同じ趣旨を1行(理由は decisions.yaml の ID 参照で)

### 受け入れ条件

- [ ] `.claude/decisions.yaml` に guard_test つきで記録されている
- [ ] その guard_test が green
- [ ] README を読んだ人が「UCR の結果はこのリポジトリに無い」と分かる

---

## 付記: 記事側で 2026-08-30 に適用済みの修正

`writing_article/articles/` に対して次を適用済み。リポジトリ側の作業でこれと矛盾する変更をしないこと。

- **01**: 符号正解率(0.500 / 0.522 / 1.000)を追記、`±` の定義を明記、
  §3.3 の TODO に「未計測。追試が要る」を明記、素案メモに図1右下・図3の再生成不可を警告として追加
- **02**: 結論テーブルの washout 行を実測(❌ 効かない)で確定、
  §3.2 に境界の ρ の表を追加、§3.3 に τ_1e / τ_integrated の比較表を追加、
  §3.4 を「効く」から「効かない(交絡は訓練データ量)」へ書き換え、
  §4「意外だった点」を「しきい値を4桁動かしても境界が動かない」で確定
- **03**: `±` と 3.3 倍の値の正本を明記、素案メモの再照合を完了に
- **04**: 結論テーブルの3態マップ行を実測で確定、§3.5 に3態の実測表と
  「周期軌道は 320 点中 2 点」を追記、図2 がレプリケート1本であることを本文と
  キャプションに明記、`±` の定義を明記
- **05**: **UCR の記述を全削除**し MGAB 単独の実験として書き直し。
  タイトルから「実データ」を削除、対照の数え方を「6系統・本命1・対照5」に統一、
  限界節に MGAB が合成であること・UCR を回さなかった理由・15 対しかないことを追記
